# 复制为 .env 后填入真实值（docker-compose 自动读取）
# 首次启动会用下面的账号创建管理员；数据库已存在管理员后此配置不再生效

ADMIN_USERNAME=admin
ADMIN_PASSWORD=请改成强密码-不少于12位

# 若前置 Nginx/Caddy 已提供 HTTPS，改为 true 使 Cookie 仅走安全通道
SECURITY_COOKIE_SECURE=false

LOG_LEVEL=info
LOG_FORMAT=json
